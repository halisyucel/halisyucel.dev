import type { NextPage } from 'next';
import Layout from '../components/layout';
import React from 'react';

const Home: NextPage = () => {
	return (
		<Layout>
			<h1 className={'text-8xl font-extrabold text-gradient'}>Halis Yücel</h1>
		</Layout>
	);
};

export default Home;
